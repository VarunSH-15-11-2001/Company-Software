Overall layout - 
  -Have a selection bar to choose floor number

  -Have a layout for every floor. Every other floor can have the same layout.

  -Color coding for availability.

  -Display seating capacity per room in every floor - can make it one room per floor for simplicity.

  -Allow booking of the floor/room for a particular date. 
   Maintain a list of dates for which the floor/room has been booked. 
   If the chosen date is in the list, prompt an error saying the 
   floor/room has already been booked.
  
  -Will be using CSS modules for styling each floor separately.